<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_hkd_branding_from_static" model="ir.actions.server">
        <field name="name">HKD: Áp logo &amp; nền từ static/img (ghi đè)</field>
        <field name="model_id" ref="base.model_res_company"/>
        <field name="groups_id" eval="[(4, ref('base.group_system'))]"/>
        <field name="binding_model_id" ref="base.model_res_company"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = env['res.company'].hkd_load_branding_from_module_static(force=True, companies=records)
else:
    action = env['res.company'].hkd_load_branding_from_module_static(force=True)
        </field>
    </record>
</odoo>
